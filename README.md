安装：
```
cd mineflayer
npm install mineflayer ws
cd ..
pip install fastapi uvicorn websockets pydantic python-dotenv openai
```

启动：
1. 启动游戏
2. 在一个窗口里 `
python -m agent.core
`
3. 在另一个窗口里 `
node mineflayer/index.js
`
